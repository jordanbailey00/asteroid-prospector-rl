services:
  trainer:
    image: ${TRAINER_IMAGE:-jordanbailey00/rl-puffer-base:py311-puffer2.0.6}
    build:
      context: ..
      dockerfile: infra/trainer/Dockerfile
    working_dir: /workspace
    volumes:
      - ..:/workspace
    environment:
      PYTHONUNBUFFERED: "1"
