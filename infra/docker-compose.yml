services:
  trainer:
    build:
      context: ..
      dockerfile: infra/trainer/Dockerfile
    working_dir: /workspace
    volumes:
      - ..:/workspace
    environment:
      PYTHONUNBUFFERED: "1"
