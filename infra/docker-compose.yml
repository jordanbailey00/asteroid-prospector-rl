services:
  trainer:
    image: ${TRAINER_IMAGE:-jordanbailey00/rl-puffer-base:py311-puffercore3.0.17}
    build:
      context: ..
      dockerfile: infra/trainer/Dockerfile
    working_dir: /workspace
    volumes:
      - ..:/workspace
    environment:
      PYTHONUNBUFFERED: "1"
