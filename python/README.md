# python

Python-facing environment package for Asteroid Belt Prospector.

M0 includes a contract-only `HelloProspectorEnv` stub with:
- observation shape `(260,)`
- `action_space.n == 69`
- Gymnasium-style `(obs, reward, terminated, truncated, info)` returns
